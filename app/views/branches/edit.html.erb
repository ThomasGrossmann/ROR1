<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing <%= @branch.name %></h1>

  <%= render "form", branch: @branch %>

  <%= link_to "Show this branch", @branch, class: button_style('show') %>
  <%= link_to "Back to branches", branches_path, class: button_style('back') %>
</div>
