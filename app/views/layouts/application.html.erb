<!DOCTYPE html>
<html>
  <head>
    <title>Ror1EsDev</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <header class="top-0 sticky p-4 flex justify-between bg-slate-400">
        <nav>
          <ul class="flex items-center gap-4">
            <li class="<%= is_active?('home') %>">
              <%= link_to "Home", home_path %>
            </li>
            <li class="<%= is_active?('school_classes') %>">
              <%= link_to "School Classes", school_classes_path %>
            </li>
            <li class="<%= is_active?('students') %>">
              <%= link_to "Students", students_path %>
            </li>
            <li class="<%= is_active?('branches') %>">
              <%= link_to "Branches", branches_path %>
            </li>
            <li class="<%= is_active?('teachers') %>">
              <%= link_to "Teachers", teachers_path %>
            </li>
            <li class="<%= is_active?('notes') %>">
              <%= link_to "Notes", notes_path %>
            </li>
          </ul>
        </nav>
        <div>
          <a href="/register">Register</a>
          <span>|</span>
          <a href="/login">Sign In</a>
        </div>
      </header>
    <main class="container mx-auto mt-28 px-5 flex flex-col">
      <%= yield %>
    </main>
  </body>
</html>
